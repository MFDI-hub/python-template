# Read the Docs config for MkDocs hosting (optional alternative to GitHub Pages).
# Enable the project at https://readthedocs.org/ after pushing this file.
# https://docs.readthedocs.io/en/stable/config-file/v2.html

version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: true

python:
  install:
    - requirements: docs/requirements-rtd.txt
